language: cpp

matrix:
  include:
    - os: linux
      dist: xenial
      compiler: gcc
      
    - os: linux
      dist: xenial
      compiler: clang

    - os: osx
      osx_image: xcode10.1
      
script:
- mkdir build
- cd build
- cmake ..
- make
